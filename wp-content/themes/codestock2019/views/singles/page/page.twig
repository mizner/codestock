{#{{ dump() }}#}
{% set base = 'SinglesPage' %}
<div class="{{ base }}">
    <h1 class="screen-reader-text">{{ post.post_title }}</h1>
    <div class="{{ base }}__content">{{ the_content() }}</div>
</div>
