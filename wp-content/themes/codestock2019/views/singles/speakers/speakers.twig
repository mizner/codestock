{#{{ dump() }}#}
{% set base = 'SinglesSpeakers' %}
<section class="{{ base }}">
    <div class="container">
        <header>
            <a href="{{ archive_link(post.post_type) }}">
                <span>Back</span>
            </a>
            <img alt="{{ image.alt }}" src="{{ image.url }}">
        </header>
        <article>
            <h1>{{ post.post_title }}</h1>
            <div class="{{ base }}__content">{{ the_content() }}</div>
        </article>
    </div>
</section>
